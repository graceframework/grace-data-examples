rootProject.name = 'Grace.Data.Examples'

// Examples
file("${rootDir}/examples").eachDir {
	include "examples-${it.name}"
    project(":examples-${it.name}").projectDir = new File(settingsDir, "examples/${it.name}")
}
